import required from './required';
import email from './email';
import regex from './regex';
import min from './min';
import max from './max';

export default {
    required,
    email,
    regex,
    min,
    max
};
